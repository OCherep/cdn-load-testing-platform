# CDN Load Platform — FIRST START GUIDE

Цей документ призначений для **абсолютного новачка**.
Він описує **повний шлях**: від чистого ноутбука → до **живого тестування CDN**.

---

## 📌 Що робить цей проєкт

* Створює EC2 інстанси для навантаження
* Генерує тисячі / десятки тисяч HTTP-сесій
* Тестує CDN (FO + Edge)
* Показує live-графіки
* Підтримує Chaos / Canary / Blue-Green
* Автоматично зупиняється (cost-guard)

---

## 🧱 Архітектура (коротко)

```
[Browser]
   ↓
[Controller EC2]
   ├── Web UI
   ├── API
   ├── Prometheus
   ├── Grafana
   ↓
[DynamoDB] ← state / chaos / progress
   ↓
[Load Agents EC2] → CDN (FO / Edge)
```

---

## 1. Що потрібно ПЕРЕД початком

### 1.1. AWS account

Потрібен **AWS акаунт** з правами:

* EC2
* DynamoDB
* S3
* IAM
* SSM

---

### 1.2. На локальній машині

Перевір, що встановлено:

```bash
git --version
docker --version
docker compose version
terraform version
go version
aws --version
```

---

## 2. Клонування репозиторію

```bash
git clone https://github.com/YOUR_ORG/cdn-load-platform.git
cd cdn-load-platform
```

---

## 3. AWS credentials

### 3.1. Створення AWS профілю

```bash
aws configure --profile cdn-load
```

Введи:

```
Access Key
Secret Key
Region (наприклад eu-central-1)
```

Перевір:

```bash
aws sts get-caller-identity --profile cdn-load
```

---

## 4. Single-command deploy 🚀

### 4.1. Один запуск — ВСЕ розгортається

```bash
./deploy.sh
```

Цей скрипт:

1. Створює S3 bucket
2. Завантажує demo-профілі
3. Створює Controller EC2
4. Створює Load Agents
5. Запускає Docker-контейнери

⏱ Час: \~5–7 хвилин

---

## 5. Web UI

Після deploy.sh ти побачиш:

```
Controller URL: http://X.X.X.X:8080
Grafana URL:    http://X.X.X.X:3000
```

### Логін:

```
user: admin
password: admin123
```

---

## 6. Перший тест (демо)

1. В UI обери:
   * Profile: `demo-kyivstar-hls.json`
   * Instances: 2
   * Sessions: 1000
2. Натисни **Apply**
3. Натисни **Start test**

👉 Запити реально підуть у CDN

---

## 7. Chaos testing

У UI:

* Enable Chaos
* Latency: 200ms
* Error rate: 5%
* Apply

Chaos вмикається **без рестарту агентів**

---

## 8. Grafana

Відкрий:

```
http://X.X.X.X:3000
```

Dashboards:

* Load Overview
* Per-Edge Latency
* Chaos Impact
* Cost Guard

---

## 9. Завершення та очистка

```bash
terraform destroy -auto-approve
```

(виконується автоматично при `./deploy.sh destroy`)

---

## 10. Безпека (RBAC — просто)

RBAC тут означає:

* **admin** — запускає тести
* **viewer** — лише дивиться графіки

Ніякої складної ролі — просто JWT з роллю.

---

## 11. Звіти

У UI:

```
Export → PDF / CSV
```

---

## Готово 🎉

Ти щойно:

* розгорнув інфраструктуру
* запустив живий CDN-тест
* побачив реальні edge-метрики

---
