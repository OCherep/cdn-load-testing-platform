{
  "title": "CDN Geo Latency",
  "panels": [
    {
      "type": "timeseries",
      "title": "Latency per Region (ms)",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(cdn_latency_ms_bucket[1m])) by (le, region))",
          "legendFormat": "{{region}}"
        }
      ]
    }
  ],
  "schemaVersion": 37,
  "version": 1
}
